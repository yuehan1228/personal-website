# Netlify 配置文件

[build]
  # 本项目是静态网站，无需构建
  publish = "."

[[redirects]]
  # 将所有请求重定向到 index.html（用于单页应用）
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  # 构建环境变量（如果需要）
  NODE_VERSION = "18"

